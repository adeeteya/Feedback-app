name: feedback_app
description: A Feedback App created using flutter.
publish_to: 'none'
version: 1.5.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flare_flutter: ^3.0.2

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
   - assets/
